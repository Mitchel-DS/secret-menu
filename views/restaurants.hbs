<main id="restaurants">
	{{#each restaurants}}
	<article>
		<h2>{{name}}</h2>
		<h3>{{category}}</h3>
		<p>{{rating}}</p>

		<form action="/details" method="post">
			<input type="submit" name="title" value="{{name}}">
		</form>
	</article>
	{{/each}}
</main>
<main id="restaurants">
	{{#each restaurants}}
	<article>
		<h2>{{name}}</h2>
		<h3>{{address}}</h3>
		<h3 class="category">{{category}}</h3>
		<p>{{rating}}</p>

		<form action="/details" method="post">
			<input type="submit" name="title" value="{{name}}">
		</form>
	</article>
	{{/each}}
</main>